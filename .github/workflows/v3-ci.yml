name: V3 CI

on:
  push:
    branches:
      - main
      - "feature/research-workbench-v3*"
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Python syntax
        run: python -m compileall -q atlas serve_atlas.py
      - name: V3 semantic contract
        run: python -B tests/test_semantic.py
      - name: V3 SEC starter transform
        run: python -B tests/test_sec_starter.py
      - name: V3 10-K filing-text semantics
        run: python -B tests/test_filing_text.py
      - name: V3 guided workflow
        run: python -B tests/test_v3.py
      - name: V3-7 company product map
        run: python -B tests/test_company_map.py
      - name: V3-8 industry driver modules
        run: python -B tests/test_industries.py
      - name: V3-10 optional AI planner
        run: python -B tests/test_planner.py
      - name: V3 frontend syntax
        run: node --check workbench/v3.js && node --check workbench/v3-charts.js
      - name: Existing research tests
        run: python -B tests/test_research.py
      - name: Existing workbench tests
        run: python -B tests/test_workbench.py
      - name: Legacy deterministic model tests
        run: node --test tests/model.test.cjs
      - name: Install browser E2E dependencies
        run: |
          npm install --no-audit --no-fund
          npx playwright install --with-deps chromium
      - name: Browser E2E — Claim Workspace / provenance
        run: |
          rm -f .runtime/e2e.sqlite3
          python -B serve_atlas.py --db .runtime/e2e.sqlite3 > /tmp/atlas-e2e.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT
          ready=0
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:8766/api/session >/dev/null; then ready=1; break; fi
            sleep 1
          done
          if [ "$ready" != "1" ]; then cat /tmp/atlas-e2e.log; exit 1; fi
          npm run test:e2e
